name: Main
on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14.x"
      - name: Install dependencies
        run: npm install
      - name: Run test
        run: npm test

  deploy:
    runs-on: ubuntu-latest
    needs: [build]
    steps:
      - uses: actions/checkout@v2
      - uses: dilip20222/heroku-deploy@v3.12.12
        with:
          heroku_api_key: 808a16b0-1eaa-4a37-9a52-c8a847ca6f38
          heroku_app_name: "herokutest1200"
          heroku_email: "suthardips123000@gmail.com"
